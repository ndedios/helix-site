version: 1

indices:
  english: &default
    include:
      - /articles/*
    target: /articles/query-index.xslx
    properties:
      title:
        select: main h1:first-of-type
        value: |
          textContent(el)

